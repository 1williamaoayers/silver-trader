# Dockerfile for ARMv7/ARM64 (OneCloud/OpenWrt compatible)
# Uses a very lightweight Nginx image

FROM nginx:alpine

# Remove default Nginx website
RUN rm -rf /usr/share/nginx/html/*

# Copy the static website files to the container
# (Ensure you have uploaded the 'dist' folder to the same directory as this Dockerfile)
COPY dist /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]
